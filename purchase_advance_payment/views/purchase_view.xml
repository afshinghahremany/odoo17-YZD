<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_view_picking']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//button[@name='action_rfq_send']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//button[@name='print_quotation']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
             <xpath expr="//button[@name='action_rfq_send']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>           
            <button name="button_draft" position="after">
               <button
                name="action_auto_advance_payment"
                string="پرداخت به تامین کننده"
                type="object"
                groups="account.group_account_invoice"
                invisible="state not in ['purchase', 'done'] or not amount_residual"
                class="btn-primary"/>
            </button>
            <xpath expr="//header/button[@name='button_confirm']" position="after">
                <button
                    name="action_next_stage"
                    string="تغییر مرحله"
                    type="object"
                    invisible="state not in ['purchase', 'done'] or not next_stage_name"
                    class="btn-primary"/>
                <div class="oe_inline">
                    <span class="badge badge-success" style="font-size: 14px; margin-right: 8px;">
                        <field name="next_stage_name"  invisible="state not in ['purchase', 'done']" readonly="1"/>
                    </span>
                </div>
            </xpath>
            <notebook position="inside">
                <page string="لیست پرداخت ها" groups="account.group_account_invoice">
                    <field
                        name="account_payment_ids"
                        nolabel="1"
                        colspan="4"
                        context="{'form_view_ref': 'account.view_account_payment_form','tree_view_ref': 'account.view_account_payment_tree'}"
                    />
                </page>
            </notebook>
            <field name="tax_totals" position="after">
                <div class="oe_subtotal_footer_separator oe_inline o_td_label">
                    <label for="amount_residual" />
                </div>
                <field
                    name="amount_residual"
                    nolabel="1"
                    class="oe_subtotal_footer_separator"
                    widget="monetary"
                    options="{'currency_field': 'currency_id'}"
                />
            </field>
        </field>
    </record>

    <record id="purchase_order_view_tree" model="ir.ui.view">
        <field name="name">purchase.order.view.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="advance_payment_status"
                    decoration-success="advance_payment_status == 'paid'"
                    decoration-info="advance_payment_status == 'partial'"
                    decoration-warning="advance_payment_status == 'not_paid'"
                    widget="badge"
                    optional="hide"
                />
            </field>
            <field name="amount_total" position="after">
                <field
                    name="amount_residual"
                    sum="Amount Residual"
                    widget="monetary"
                    decoration-bf="1"
                    optional="hide"
                />
            </field>
        </field>
    </record>

    <record id="purchase_order_kpis_tree" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.tree</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="advance_payment_status"
                    decoration-success="advance_payment_status == 'paid'"
                    decoration-info="advance_payment_status == 'partial'"
                    decoration-warning="advance_payment_status == 'not_paid'"
                    widget="badge"
                    optional="hide"
                />
            </field>
            <field name="amount_total" position="after">
                <field
                    name="amount_residual"
                    sum="Amount Residual"
                    widget="monetary"
                    decoration-bf="1"
                    optional="hide"
                />
            </field>
        </field>
    </record>

</odoo>
